[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "testbed"
version = "0.1.0"
description = "BlueFlow Testbed — reproducible test environment for passive and active scanning pipelines"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "BlueFlow Team" }]
keywords = ["blueflow", "testbed", "dicom", "hl7", "security"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
]
dependencies = [
    "typer>=0.12.0",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "rich>=13.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "radon>=6.0",
    "ruff>=0.6",
    "prospector>=1.7",
]

[project.scripts]
testbed = "testbed.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]

[tool.radon]
# Cyclomatic complexity: A=1-5, B=6-10, C=11-20, D=21-30, E=31+
# Aim for simple, pythonic code — avoid deep nesting
cc_min = "B"
ignore = "tests"

[tool.ruff]
line-length = 100
target-version = "py311"
