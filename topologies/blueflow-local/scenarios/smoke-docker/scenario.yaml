name: smoke-docker
topology: topologies/blueflow-local/topology.yaml

facts: []

nodes:
  - id: postgres
    build: up
    networks: [clinical-vlan]
  - id: redis
    build: up
    networks: [clinical-vlan]
  - id: blueflow
    build: up
    networks: [clinical-vlan]
  - id: celery-worker
    build: up
    networks: [clinical-vlan]
  - id: tapirx
    build: up
    networks: [clinical-vlan]
  - id: pacs-server
    build: up
    networks: [clinical-vlan]

commands: []
